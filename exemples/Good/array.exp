/* Déclaration de variable array se fait par valeur (copie) */
/* int */

let
    var N := 2

    type intArray = array of int
    type intArrayArray = array of intArray

    var row := intArray[N] of 0
    var board := intArrayArray[N] of row
in
    print(board[1][1]);

    board[1][1] := 1;
    print(board[2][1])
end
